namespace Workes.SaveSystem
{
    /// <summary>
    /// Marker interface for types that identify save slots.
    /// Implement this interface on types used to distinguish between different save files.
    /// </summary>
    public interface ISaveIdentity
    {
    }
}
